/* Deprecated stub.
 * Older builds pointed here. Current entry is content/main-classic.js.
 * Safe no-op if a stale Chrome cache still requests this path.
 */
(function () {
  console.info("[Local ATS] content/content.js is deprecated; active script is main-classic.js. Remove + re-load the extension if you still see import errors.");
})();
